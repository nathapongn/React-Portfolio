import Copy from "../primitives/Section/Copy.jsx";

export default function SectionHeader(props){
    return(
        <Copy 
            fullWidth
            className="section-header"
            {...props}
        />
    )
}